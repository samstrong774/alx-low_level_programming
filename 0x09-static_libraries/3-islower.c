#include "main.h"
/**
 * _islower - search for lowercase character
 * @c: is the character for the search
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
